# PROBE:把冻结VLA当作测量仪器的能力场编排
## (Probing-based Orchestration for VLA-as-Tools)

---

# 第一部分:详细思路说明

## 1. 研究问题与动机

### 1.1 要解决的具体问题

在"冻结VLA作技能原语、LLM/VLM agent做编排"这一范式中,编排agent关于"某个VLA工具在当前条件下能不能完成某个子任务"的知识,只能通过两条途径获得:

1. **离线先验**:如 VLAs-as-Tools (arXiv:2605.13119) 的 Tool-Aligned Post-Training、Cortex (arXiv:2607.05377) 的32个规范化技能原语——部署前一次性固化,部署后不更新;
2. **付出物理代价的执行记录**:如 Harness VLA (arXiv:2607.08448) 从执行轨迹统计 operating range、ETA/OpenETA (arXiv:2608.03924) 的成败经验库、BATON (arXiv:2608.16889) 的子任务级物理探索、Agentic-VLA (arXiv:2605.22896) 的在线RL rollout。

结果是:每一条"这个工具在这种条件下会失败"的知识,都对应一次真实的物理失败(掉落、碰撞、时间成本),而且这些知识多为 task-specific 经验规则,跨任务要重新交学费。这直接导致该主线在"失败检测与恢复"上持续内卷(HELM、RoboBRIDGE、VoLo、ProbeAct、ReCoVLA、B2FF、CoRe 等一大批工作),因为**信息流拓扑决定了失败是编排层学习工具能力的唯一渠道**。

此外,后续迭代检索确认了第三条已成型的通道——**世界模型想象rollout**(τ0-WM arXiv:2606.01027、Cortex 2.0 arXiv:2604.20246、STORM arXiv:2512.18477、τ0-VLA arXiv:2608.16885、Veo生成式评测 arXiv:2512.10675):在执行前用学习的动力学模型预测候选动作/子任务的后果。但它测量的是"世界对给定动作的响应"而非"工具自身的能力",需要训练昂贵的动力学模型,且对非专家动作的action following保真度不可靠(WorldEcho, arXiv:2608.24885);同样没有进入编排层的反事实条件查询与价值-信息权衡。

本工作要解决的问题是:**让编排agent在物理行动之前,以零物理成本主动获取VLA工具的能力信息,并把"获取信息"本身纳入规划决策。**

### 1.2 为什么现有工作没有解决

两条证据链说明"能力信息其实唾手可得,但没人给编排层开这个通道":

- **能力信息存在于冻结VLA的输出分布中**:SVA (arXiv:2607.03751) 的 pass@k 诊断显示冻结VLA成功率从 pass@1 的 33% 升到 pass@32 的 92%;"Reuse Before You Retrieve" (arXiv:2608.17484) 用 recoverable headroom 定量刻画了这一点;arXiv:2608.13474 进一步证明任务进度可从 π0.5 残差流线性解码。
- **执行前读取可靠性信号已被证明可行**:Pre-VLA (arXiv:2605.22446) 的 preemptive action validity assessment、FabriMAE (arXiv:2608.16697) 的注意力熵自评估、VeriSpace (arXiv:2606.10568)、LM-X (arXiv:2608.25757)、GUARD (arXiv:2608.04510) 的 KV cache 反事实消融、AEGIS (arXiv:2606.06660) 的激活探针早期预警;尤其 arXiv:2605.28527 用线性探针从冻结 π0.5/OpenVLA 表征直接读出成功价值(同任务同时刻配对排序准确率约92%)。因此"单点、同任务可预测"已不是本工作的赌注——赌注在**跨反事实条件与跨任务的校准迁移**。

但检索确认(经多轮迭代收敛):上述执行前验证工作**全部是绑定当前调用的被动安全过滤器/动作级选择器**——固定触发、单点判断、判断结果只用于"放行/拦截/换动作",从不进入编排层的决策空间。没有任何工作让agent:

- 对**假想条件**(换一种指令措辞、换一个staging位姿的视图、换一个技能候选)发起反事实查询;
- 用查询结果**绘制能力场**并在其上做全局规划;
- 在"查询免费、执行昂贵"的显式价值-信息权衡下决定何时问、何时做。

而编排主线的6篇代表作(VLAs-as-Tools、ETP、Agentic-VLA、VoLo、Harness VLA、ETA)及其新扩展(RoboHarness arXiv:2607.18060 与 arXiv:2603.24060、Pigey arXiv:2607.21725)的工具接口都把"调用=物理执行"绑定为原子操作,架构上不存在非执行的查询入口。RoboHarness (2607.18060) 虽提出 capability-aware routing,但其能力边界证据仍全部来自事后执行轨迹。

概念传统上,机器人能力自评估(competency self-assessment)有成型的 pre-VLA 学术脉络(Dynamic Competency Self-Assessment arXiv:2303.01646、MUSE arXiv:2411.13537、Competency-Aware Planning arXiv:2409.06111),但均基于自带动力学模型的 outcome 分布自评;本工作首次把这一传统带入 VLA-as-tools 编排范式,且测量对象换成冻结策略自身的行为分布。

## 2. 核心洞察

> **冻结VLA不仅是执行器,还是一台可零物理成本反事实查询的测量仪器:对(指令措辞 × staging观测 × 技能选择)做不执行的前向采样,其行为分布统计量可预测执行成败;因此编排应从"执行→失败→学习"的被动循环,变为在主动实测的能力场上做价值-信息权衡的规划。**

现有三条信息通道各有结构性缺陷——(1) 离线先验:静态;(2) 事后执行记录:每条知识对应一次物理代价;(3) 世界模型想象rollout:测量的是世界对动作的响应而非工具能力,模型昂贵且保真度受限。PROBE补上第四条通道:直接读取冻结VLA自身的行为分布,并把它做成编排agent的一等行动。

这个洞察贯穿方法设计的每个环节:

| 环节 | 洞察的体现 |
|---|---|
| 工具接口 | 必须新增与 `execute` 平行的 `probe` 入口——"测量仪器"需要读数端口,这是对6篇代表作接口的结构性改动而非模块叠加 |
| 探测特征 | 特征全部取自行为分布本身(样本一致性、时间发散、措辞敏感度),而非外部VLM看像素的语义猜测——测量的是仪器自身的行为,不是环境表象 |
| 能力头 | 校准模型跨任务共享——赌注是行为分布统计量比语义规则更任务无关,这正是"测量仪器读数可复用"与Harness VLA"task-specific经验规则"之间的分界 |
| 规划器 | 行动空间显式包含查询动作,决策规则是价值-信息权衡——"提问免费、行动昂贵"这一不对称性只有在洞察成立时才存在 |
| 失败归因 | 归因优先用反事实probe(换措辞收敛→措辞问题;换staging视图收敛→staging问题),物理实验是最后手段——把归因从"猜测"变成"测量" |
| 记忆 | 存"探测特征→成败"的校准映射而非情节记录——记的是仪器的标定曲线,不是事故日志,天然跨任务 |

## 3. 方法框架

### 3.1 总体信息流

```
                       ┌──────────────────────────────┐
                       │  认知-物理混合规划器 (LLM agent) │
                       └──────┬───────────────┬───────┘
                    probe查询 │               │ execute调用
                              ▼               ▼
                  ┌───────────────────────────────────┐
                  │      双模式VLA工具接口              │
                  │  probe(instr, obs, K) → 特征向量    │
                  │  execute(instr, budget) → 执行结果  │
                  └──────┬───────────────┬────────────┘
                         ▼               ▼
                  ┌────────────┐   ┌───────────┐
                  │ 能力头(校准) │◄──│ 物理世界    │ (执行成败标签在线更新校准)
                  └──────┬─────┘   └───────────┘
                         ▼
                  ┌────────────┐
                  │ 能力场记忆   │ → 反哺规划器的调用方案比较与失败归因
                  └────────────┘
```

### 3.2 模块一:双模式工具接口

- **输入/输出**:
  - `execute(instruction, budget)` → 执行轨迹 + 新观测(原有,与6篇代表作一致);
  - `probe(instruction, obs, K)` → 探测特征向量。`obs` 可以是当前真实观测,也可以是**假想观测**(解析原语可达的某个staging位姿的渲染/裁剪视图)。
- **探测特征库**(每项都是行为分布的统计量):
  1. K条action chunk的成对DTW距离/方差(行为一致性);
  2. chunk内时间发散速率(聚合采用**尖峰保留**而非均值——均值会稀释短时安全关键不确定性尖峰,依 arXiv:2603.18342);
  3. 首步动作与解析可达域的重叠度;
  4. 对M个指令措辞改写的分布敏感度(利用 VLA Grounder (arXiv:2607.04517) 发现的措辞敏感性,反过来当测量维度;措辞查询集可用 Q-DIG (arXiv:2603.12510) 式 Quality-Diversity 机制生成以保证覆盖多样性);
  5. (可选)GUARD式KV cache反事实grounding分;
  6. (可选)arXiv:2605.28527 式内部表征线性探针读数。
- **设计理由**:probe不下发机器人指令,K次前向是纯GPU成本;特征不依赖VLA架构内部细节(1-4对任何输出action chunk的VLA通用),保证跨底座可迁移。

### 3.3 模块二:轻量能力头(Competence Head)

- **输入**:探测特征向量;**输出**:校准的成功概率 + 置信区间。
- 实现为梯度提升树或2层MLP,用少量带成败标签的执行数据训练,**跨任务共享**,随部署中的执行结果在线更新。
- **设计理由**:轻量意味着标签效率高(百量级执行样本可训);跨任务共享是与Harness VLA的本质区别——如果行为分布统计量只在单任务内有预测力,核心假设即告失败,所以这一设计选择同时是假设检验的载体。
- **OOD"自信地错"的缓解**:对观测做小扰动(视角/光照/裁剪抖动)测分布稳定性,把"一致但错"转化为可检测的脆性信号。

### 3.4 模块三:认知-物理混合规划器

- **行动空间** = {物理动作(解析原语、VLA execute)} ∪ {probe查询}。
- **决策规则**:采用成本感知的期望信息增益(EIG)形式化——方法论先例为 Active Experiment Selection (arXiv:2502.09829),但把EIG查询选择从实验者侧的**物理试验**移入agent侧的**零物理成本前向查询**,并置于任务内闭环。候选调用方案(措辞×staging×技能的组合)间预测成功率差距大或置信高 → 直接执行;否则先做一批probe绘制局部能力场,选场上高点执行。查询预算由推理延迟约束限定。
- **失败归因协议**:失败后先跑反事实probe矩阵——只换措辞、只换staging视图、只换技能候选,观察哪一维使分布收敛;probe不可区分时才做受控物理试验。
- **设计理由**:这是洞察进入决策空间的地方。被动过滤器(Pre-VLA等)回答"这一个调用行不行";混合规划器回答"哪一个调用方案最值得做、还是应该再问一轮"。

### 3.5 模块四:能力场记忆

- **存储内容**:校准后的"探测特征→成败"映射及其在线更新状态,附任务/场景元信息。
- **区别于ETA式情节记忆**:不存"任务X第3步失败过",存的是标定曲线,检索时按特征空间近邻而非任务标签,天然跨任务迁移。

### 3.6 实验设计(承自迭代终版)

- **环境**:LIBERO-Long / LIBERO-Pro(对齐Harness VLA、HELM)、RoboTwin 2.0(对齐VLAs-as-Tools)、BEHAVIOR-1K链式初态协议(arXiv:2607.06256);VLA底座 π0.5 与 OpenVLA 各一。
- **Baseline**:Harness VLA(事后operating range vs 事前probe)、BATON复现、VoLo式VLM监控编排、OpenETA、**世界模型评估器**(τ0-WM式打分或视频WM,替换probe通道做同预算对比,直接比较预测力/成本比;代表作另见 Veo 生成式评测 arXiv:2512.10675)、以及关键消融——同框架去掉probe通道只留执行反馈。探测有效性实验中,arXiv:2605.28527 的线性探针与 **Reuse Before You Retrieve (arXiv:2608.17484) 的 recoverable headroom 度量 + episode-level retry selector** 同为必比基线——后者代表"执行期重试选择"这一路线,与本工作的"执行前反事实探测"在同等采样预算下正面对比预测力与物理成本。
- **指标**:
  1. 探测有效性:probe特征预测执行成败的AUROC/ECE,含跨任务zero-shot校准;
  2. 任务级:长程成功率;
  3. **物理交互经济性**(核心差异化指标):达到同等成功率所需物理步数/重试次数/碰撞次数;报告成功率-物理成本 Pareto 曲线,协议参考 DIRECT (arXiv:2606.12402)——本工作补充其未覆盖的"GPU算力↔物理交互"跨域权衡维度;
  4. 归因准确率:注入式扰动(措辞/staging/能力缺失)下probe归因 vs VLM像素归因。真机评测协议参考 ManipArena (arXiv:2603.28545)。
- **假设检验逻辑**:指标1不成立 → 核心假设被证伪;1成立而2、3不成立 → 探测有信号但规划器未用好。

## 4. 与最相关工作的边界

- **vs 事后能力统计:Harness VLA (arXiv:2607.08448)、RoboHarness (arXiv:2607.18060 capability-aware routing / arXiv:2603.24060 归因驱动编排)**:它们的能力边界与归因全部来自已付出代价的执行轨迹/记忆,且多为task-specific规则;PROBE是执行前零物理成本测量、跨任务共享校准。不是"给Harness加个不确定性模块"——它们的接口没有非执行通道,信息流拓扑不同。论文写作时须显式区分RoboHarness的"capability-aware"措辞。
- **vs 被动读取:Pre-VLA / FabriMAE / VeriSpace / LM-X / GUARD / AEGIS (arXiv:2606.06660) / 线性探针 (arXiv:2605.28527)**:同样读执行前/执行中可靠性信号,但它们是当前调用的被动过滤器或动作级选择器,固定触发、结果不进决策。PROBE把同类信号变成agent可对**反事实条件**发起的查询,并纳入价值-信息权衡。超出模块拼接之处:被动过滤器加进任何编排框架,回答的仍是"这个调用行不行",不会产生"比较多个假想方案、主动设计查询集"的行为——那需要行动空间和规划目标的改变。执行前验证还需细分**计划级验证**(VerifyLLM arXiv:2507.05118、SafeGate arXiv:2604.05427,验证对象是计划文本)与本工作的**调用级探测**。
- **vs 世界模型评估 / 测试时算力分配:τ0-WM (arXiv:2606.01027)、Cortex 2.0 (arXiv:2604.20246)、STORM、τ0-VLA (arXiv:2608.16885)、Veo评测 (arXiv:2512.10675),以及 embodied TTS+外置verifier的 E-TTS (arXiv:2606.27268)、ParallelWorld (arXiv:2608.22971)**:三点差异——(i) PROBE测量**冻结工具自身的行为分布**(零额外模型),世界模型/TTS评估**世界对给定动作的响应**或由外置VLM verifier对候选打分(需训练/配置昂贵verifier,且对非专家动作保真度不可靠,见WorldEcho arXiv:2608.24885);(ii) PROBE查询的是**调用条件三元组(措辞×staging×技能)**,世界模型/TTS评估的是动作/子任务序列或其语义正确性;(iii) probe是编排agent行动空间的一等公民,受显式价值-信息权衡支配,τ0-VLA等是端到端层级基础模型的内部搜索、E-TTS/ParallelWorld是每次决策都做的闭环采样-打分迭代。E-TTS的"历史感知verifier闭环迭代"与PROBE的闭环无关——PROBE的闭环是规划器的EIG权衡,不是逐次调用的verifier循环。实验含世界模型评估器baseline正面对比。
- **vs SVA (arXiv:2607.03751)**:SVA离线MCTS蒸馏Q模型在**动作级**选样,无agent、无反事实条件比较;PROBE在**编排级**在线比较调用方案。SVA同时是核心假设的正面证据来源。
- **vs Reuse Before You Retrieve (arXiv:2608.17484)——目前最近的一篇,必须显式划界**:它用 recoverable headroom 与 retrieval complementarity 两个可测因子,量化"冻结VLA的随机rollout里已有多少可恢复能力、何时该改用外部动作先验",并以 episode-level retry selector 在retryable/parallel执行下把潜在能力捞出来(LIBERO上最高+21.0成功率点,且跨机器人/仿真器迁移、在退化观测下仍有效)。这直接坐实了PROBE的两条前提——能力信息确实存在于冻结策略的行为分布中、且重采样+选择可将其变现。因此论文写作时**不能**把"行为分布可预测成败"当作本工作的空白点,须与SVA、arXiv:2605.28527并列为已有正面证据。两点差异使其不覆盖PROBE:(i) 它是**执行期/事后的候选重试选择器**(先执行、失败或并行后再选),测量与决策都绑定在当前调用的rollout集合上,不存在对**假想条件**的非执行查询;(ii) 它在**轨迹/episode级**排序候选rollout,不在**调用条件三元组(措辞×staging×技能)**上比较方案,也无跨任务共享的能力标定。据此PROBE的赌注收敛为**跨反事实条件与跨任务的校准迁移 + 探测入编排决策空间的价值-信息权衡**,而非"行为分布是否含成败信号"。该工作同时应作为探测有效性实验的对照(其headroom度量 vs 本工作的能力头预测),其OpenVLA实验区分的"headroom存在"与"能否排序候选"也正对应本工作的"自信地错"风险。
- **vs BATON (arXiv:2608.16889)**:BATON把长程探索成本从T^K降到T·K,但每次探索仍是物理执行;PROBE把大部分探索移入查询空间,物理成本目标为亚线性。
- **vs VoLo (arXiv:2606.07723) / ETA (arXiv:2608.03924) / Pigey (arXiv:2607.21725)**:其信息全部产生于执行中/后(中断式steering、执行后验证);PROBE增加执行前的主动认知通道,与其恢复机制正交且可叠加。
- **vs 测试时算力分配:DIRECT (arXiv:2606.12402)、ELASTIC、DASIP**:它们在"GPU算力↔成功率"内做分配;PROBE新增"GPU算力↔物理交互"跨域权衡维度。
- **vs 主动测试:Active Experiment Selection (arXiv:2502.09829)**:成本感知EIG选实验的方法论先例,但它是实验者侧的离线评测,每个查询仍是一次物理rollout;PROBE把EIG移入agent侧、任务内、零物理成本的前向查询。**vs Q-DIG (arXiv:2603.12510)**:QD搜索措辞失败面靠执行rollout取标签,离线红队;PROBE的措辞探测无需执行。
- **概念传统**:competency self-assessment(arXiv:2303.01646、MUSE arXiv:2411.13537、arXiv:2409.06111)首次进入VLA-as-tools编排范式,测量对象从自带动力学模型换成冻结策略行为分布;信息获取成本的决策论框架另见 Cognitive Friction (arXiv:2603.30031,非具身同构)。

## 5. 预期贡献

主要贡献落在**方法**,附带一项**实证/benchmark贡献**:

1. **方法(核心卖点)**:首个把"探测冻结VLA能力"提升为编排agent第一类行动的架构——双模式工具接口 + 跨任务能力头 + 认知-物理混合规划器,并展示由此带来的物理交互经济性优势(同等成功率下显著更少的物理步数/失败次数)。
2. **实证**:系统回答"冻结VLA的行为分布统计量在多大程度上、跨多大条件范围内可预测执行成败"——即使部分为负(如OOD下自信地错不可校准),对SVA/headroom这条实证线也是重要结果。
3. **benchmark侧产出(次要)**:反事实probe归因协议与注入式扰动归因评测,可复用于后续编排工作。

---

# 第二部分:3个月执行时间规划

**目标产出级别(明确)**:第3个月末交付**一份可投稿的初版论文**(目标 CoRL / ICRA / ICLR,视截稿窗口)+ 配套可复现代码仓库。仿真实验完整,真机部分按"有则加分"处理(见月3周3的裁剪线)。

## 月1:探测有效性验证(核心假设的生死关)

本月唯一目标:回答"probe特征能不能预测执行成败"(实验指标1)。这是全项目最大风险所在,必须最先做、做扎实。

### 周1
- **产出**:LIBERO-Long + π0.5 checkpoint 环境跑通;批量rollout采集脚本(记录每次调用的观测、指令、K=32采样的action chunk、执行成败标签);首批 ≥500 条带标签调用数据。
- **完成判定**:脚本可无人值守跑通宿主任务全集;数据含成败两类且失败样本占比≥20%(不足则加扰动注入)。
- ⚠️ **风险窗口**:π0.5/LIBERO复现不顺(依赖冲突、checkpoint不可得)。**触发条件**:周1末尚无法批量rollout。**Plan B**:立即切换 OpenVLA + LIBERO(社区复现最成熟),π0.5改为月2再补第二底座。

### 周2
- **产出**:探测特征提取库(特征1-4:DTW方差、时间发散、可达域重叠、措辞敏感度)+ 能力头v0(梯度提升树);**同任务内**成败预测的AUROC/ECE数字与ROC图。
- **完成判定**:同任务内 AUROC ≥ 0.75(留出集)。
- ⚠️ **关键风险窗口(全项目最高)**:若AUROC在0.55-0.65之间徘徊。**触发条件**:周2末同任务AUROC<0.7。**Plan B(按序尝试,限一周)**:(a) 加大K、加入观测扰动脆性特征;(b) 加入GUARD式KV特征(需白盒);(c) 特征在子任务类型内分层校准。若周3末仍<0.7 → 核心假设接近证伪,项目转向为"负面结果+诊断分析"论文(仍有发表价值,对标headroom实证线),砍掉月2的规划器开发。

### 周3
- **产出**:**跨任务**zero-shot校准实验(任务A训练能力头、任务B测试,全排列);跨底座实验启动(第二个VLA);"自信地错"专项分析——OOD扰动下失败样本中低方差(高自信)比例的量化表。
- **完成判定**:跨任务AUROC相对同任务的衰减≤0.1;"自信地错"样本占失败样本比例有明确数字,且观测扰动脆性特征能召回其中≥50%。
- ⚠️ **风险窗口**:跨任务迁移大幅衰减(AUROC掉到0.6以下)。**Plan B**:能力头改为"少样本快速适配"设定(每新任务≤20条标定执行),论文主张从zero-shot降级为few-shot标定——弱化但不推翻核心假设,方法框架不变。

### 周4
- **产出**:假想观测probe的保真度实验——staging前用渲染/裁剪视图probe vs staging后真实视图probe,两者预测的一致性(相关系数、AUROC差);月1结果整理成论文第4.1节草稿(探测有效性)+ 2张主图。
- **完成判定**:假想/真实probe预测一致性相关系数≥0.6;草稿与图表可直接进论文。
- ⚠️ **风险窗口**:假想观测gap过大(相关<0.4)。**Plan B**:方法退化为"每次staging后、执行前probe"(丧失全局能力场规划卖点,保留调用方案比较与归因卖点),月2的规划器相应简化——此裁剪不影响可投稿性。

## 月2:混合规划器与端到端系统

前提:月1核心假设存活。本月目标:把探测接入编排决策,做出端到端长程任务系统。

### 周1
- **产出**:双模式工具接口定型(API文档+实现);LLM规划器v0——支持probe/execute混合行动空间,决策规则为固定阈值版价值-信息权衡(置信<τ则probe,否则execute)。
- **完成判定**:LIBERO-Long任一长程任务上端到端跑通≥10个episode,日志完整记录每次probe/execute决策及依据。

### 周2
- **产出**:候选方案枚举与比较模块(措辞改写×staging候选×技能候选的probe矩阵);失败归因协议实现(反事实probe矩阵+归因判定规则);能力场记忆v0(特征近邻检索+在线校准更新)。
- **完成判定**:注入式扰动归因实验(措辞扰动/staging扰动/能力缺失三类各≥30例)中probe归因准确率>VLM像素归因基线≥15个百分点。
- ⚠️ **风险窗口**:probe延迟不可接受(K×M次前向使单步决策>30s)。**触发条件**:周2末端到端episode时长超无probe基线3倍。**Plan B**:特征早停(方差估计在K=8收敛即停)、措辞改写降为M=3、probe矩阵改为规划器按信息增益排序的贪心截断。

### 周3
- **产出**:baseline阵列跑通——去probe消融(最关键)、Harness VLA(官方或复现)、OpenETA、VoLo式VLM监控、**世界模型评估器对照**(τ0-WM式打分替换probe通道,同预算比较预测力与成本);LIBERO-Long/Pro全量对比实验第一轮结果表(成功率+物理交互经济性)。
- **完成判定**:PROBE vs 去probe消融在成功率或物理步数上有统计显著差异(≥3 seed,p<0.05)。
- ⚠️ **关键风险窗口(第二高)**:探测有信号但系统级不涨(指标1好、指标2/3平)。**触发条件**:周3末与消融无显著差异。**Plan B**:诊断规划器——固定阈值换为期望效用显式计算;若仍平,论文重心移到归因准确率与探测有效性(4.1/4.4节),系统级作为"当前规划器下的初步结果"报告,并如实分析。

### 周4
- **产出**:RoboTwin 2.0 第二环境全量实验;BEHAVIOR-1K链式初态协议实验启动;方法节(第3节)完整草稿+系统框图。
- **完成判定**:RoboTwin上主结论(经济性优势)方向一致;方法节草稿经一轮自查无未定义符号。

## 月3:补全实验、消融与成稿

### 周1
- **产出**:消融矩阵——特征组消融(去DTW/去措辞敏感度/去脆性)、K值扫描、能力头容量扫描、查询预算扫描(probe次数 vs 收益曲线,这条曲线是"价值-信息权衡"卖点的直接证据);BEHAVIOR-1K链式初态实验完成。
- **完成判定**:每项消融≥3 seed;查询预算-收益曲线呈现明确的边际递减形态(或如实报告非单调并分析)。

### 周2
- **产出**:全部图表定稿(目标:主图4张——探测有效性、经济性对比、归因对比、预算-收益曲线;表3张);实验节(第4节)完整草稿;相关工作节完整草稿(覆盖迭代检索确认的全部边界工作)。
- **完成判定**:所有图表由脚本一键重生成;实验节数字与日志逐一核对无误。

### 周3
- **产出**:真机裁剪决策点——若仿真实验全部锁定且有≥2周富余,跑一套桌面多阶段真机任务(≥2任务×10 trial);否则明确放弃真机,论文限定为仿真+讨论。引言、洞察阐述、结论初稿。
- **完成判定**:真机做/不做的决策有书面记录及依据;论文全节齐备可通读。
- ⚠️ **风险窗口**:真机是唯一被明确标记为"可整体砍掉"的部分,不允许其挤占写作时间。**触发条件**:周3周一仿真实验仍有未收口项 → 当即放弃真机。

### 周4
- **产出**:全文打磨(claims与证据逐条对齐自查)、限制与失败分析节(如实报告月1-2触发过的Plan B及其含义)、可复现代码仓库整理(README+一键复现脚本)、内部预审并修订。
- **完成判定**:初版论文PDF完整(8页+附录),每条claim在实验节有对应数字支撑;仓库在干净环境可跑通quickstart。

## 跨月风险总览

| 风险 | 时间窗口 | 触发条件 | Plan B |
|---|---|---|---|
| 环境/底座复现失败 | 月1周1 | 周1末无法批量rollout | 切OpenVLA+LIBERO |
| **核心假设证伪(最高危)** | 月1周2-3 | AUROC<0.7且补救无效 | 转负面结果+诊断论文,砍规划器 |
| 跨任务迁移衰减 | 月1周3 | 跨任务AUROC<0.65 | zero-shot降级为few-shot标定 |
| 假想观测gap | 月1周4 | 一致性相关<0.4 | 退化为staging后probe,砍全局能力场卖点 |
| probe延迟 | 月2周2 | episode时长>3×基线 | 特征早停+贪心截断 |
| **有信号但系统不涨(次高危)** | 月2周3 | 与消融无显著差异 | 重心移归因+探测有效性 |
| 真机挤占写作 | 月3周3 | 周一仿真未收口 | 放弃真机 |

---

# 附录:迭代审视修订记录

两次迭代循环(共9轮检索、约15个角度)后三次触发收敛条件,核心假设与核心洞察未被推翻。实质与表述修改如下:

| 轮次 | 触发论文 | 修改内容 | 层级 |
|---|---|---|---|
| 循环1轮1 | Pre-VLA (2605.22446)、FabriMAE (2608.16697)、VeriSpace (2606.10568)、LM-X (2608.25757)、JEPA安全 (2608.17496) 等 | 核心洞察从"非执行通道不存在"改为"通道存在但只是被动过滤器,从未进入编排决策空间";AUROC主张降级为验证+扩展 | 实质修改 |
| 循环2轮1 | 2605.28527 (线性探针读成功价值)、RoboHarness (2607.18060)、AEGIS (2606.06660) | 单点可预测性不再是赌注,赌注移至跨条件/跨任务校准;边界表新增RoboHarness;线性探针入特征库并列为必比基线 | 表述微调 |
| 循环2轮3 | VerifyLLM (2507.05118)、SafeGate (2604.05427) | 执行前验证细分"计划级"与"调用级"两支 | 表述微调 |
| 循环2轮4 | τ0-WM (2606.01027)、Cortex 2.0 (2604.20246)、STORM (2512.18477)、WorldEcho (2608.24885) | 核心洞察补入第三条通道"世界模型想象rollout"及其缺陷;实验新增世界模型评估器baseline;边界表新增一行 | 实质修改 |
| 循环2轮5 | 2303.01646、MUSE (2411.13537)、2409.06111;2603.18342 | 定位为competency self-assessment传统首次进入VLA-as-tools范式;时间发散特征改尖峰保留聚合 | 表述微调 |
| 循环2轮6 | τ0-VLA (2608.16885)、DIRECT (2606.12402) | 与世界模型引导测试时计算的三点精确划界;经济性指标表述为"算力↔物理交互"跨域权衡,Pareto协议引DIRECT | 边界锐化 |
| 循环2轮8 | Active Experiment Selection (2502.09829)、Q-DIG (2603.12510)、Veo评测 (2512.10675) | 规划器决策规则获得成本感知EIG方法论先例(降低月2实现风险);措辞查询集可用QD机制生成;Veo入世界模型baseline代表作 | 表述微调 |
| 循环2轮9 | SkillEval等LLM技能生态、Weights-or-Skills综述 (2608.01851) | 无修改;综述反向确认probe式能力探测空白仍在 | 无 |
| 循环3轮1(用户指定论文) | E-TTS (2606.27268)、ParallelWorld (2608.22971)、Reuse Before You Retrieve (2608.17484) | E-TTS/ParallelWorld 归入"世界模型评估/测试时算力分配"边界条目并锐化三点差异(外置VLM verifier打分 vs 读冻结政策自身分布;PROBE的闭环是EIG权衡而非逐调用verifier循环);2608.17484 单列为最近邻工作并显式划界(其 recoverable headroom + episode-level retry selector 坐实"能力信息在分布内"这一前提,故不得再当作空白点;差异在执行期重试选择 vs 执行前反事实查询、轨迹级排序 vs 调用条件三元组比较),并升为探测有效性必比基线 | 边界锐化 |
